export namespace Interfaces {

    export interface Props {
        processing: string;
        error?: string;
        overallCount: number;
        currentQuery?: any;
    }

    export interface State {

    }

}