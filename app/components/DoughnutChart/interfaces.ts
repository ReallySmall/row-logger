export namespace Interfaces {

    export interface Props {
        index: number;
        rowId?: string;
        checked: boolean;
        selectAction: any;
        deselectAction: any;
    }

    export interface State {

    }

}