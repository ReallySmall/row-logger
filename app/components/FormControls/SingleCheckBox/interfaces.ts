export namespace Interfaces {

    export interface Props {
        disabled: boolean;
        id: string;
        label: string;
        input: any;
        checked: boolean;
        type: string;
        placeholder: string;
        required: boolean;
        meta: any;
    }

    export interface State {
        /* empty */
    }

}