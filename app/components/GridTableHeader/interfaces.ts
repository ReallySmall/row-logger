export namespace Interfaces {

    export interface Props {
        columnId: string;
        name: string;
        sortable: boolean;
        sortAction: any;
        width: string;
        sortDirection?: any;
    }

    export interface State {
        /* empty */
    }

}