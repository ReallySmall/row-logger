export namespace Interfaces {

    export interface Props {
        heading: string;
        authActions: any;
        isLoggedIn: boolean;
        userName: string;
    }

    export interface State {

    }

}