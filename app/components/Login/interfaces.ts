export namespace Interfaces {

    export interface Props {
        fields: any;
        formName: string;
        submit: any;
        processing: string;
        error: string;
    }

    export interface State { }

}