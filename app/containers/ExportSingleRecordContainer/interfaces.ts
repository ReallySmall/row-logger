export namespace Interfaces {

    export interface Props {
        exportActions?: any;
        detailId?: string;
        fileName: string;
        processing?: boolean;
        error?: string;
        fieldData?: any;
    }

    export interface State {
        /* empty */
    }

}