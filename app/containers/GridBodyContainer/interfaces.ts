export namespace Interfaces {

    export interface Props {
        gridActions?: any;
        pagerActions?: any;
        columns?: Array<any>;
        items?: object;
        ids?: Array<string>;
        showHeader?: boolean;
        sortable?: boolean;
    }

    export interface State {

    }

}