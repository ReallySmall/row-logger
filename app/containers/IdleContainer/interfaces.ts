export namespace Interfaces {

    export interface Props {
        authActions?: any;
        processing?: string;
        isLoggedIn?: string;
    }

    export interface State {
        /* empty */
    }

}