export namespace Interfaces {

    export interface Props {
        dispatch?: (action: any) => void;
        actions?: any;
        processing?: string;
        error?: string;
    }

    export interface State {

    }

}