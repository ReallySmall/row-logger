export namespace Interfaces {

    export interface Props {
        dispatch?: (action: any) => void;
        authActions?: any;
        errorActions?: any;
        processing?: string;
        error?: string;
    }

    export interface State {

    }

}