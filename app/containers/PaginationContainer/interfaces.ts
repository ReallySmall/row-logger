export namespace Interfaces {

    export interface Props {
        pagingActions?: any;
        pageSize?: number;
        pageCount?: number;
        currentPage?: number;
    }

    export interface State {

    }

}