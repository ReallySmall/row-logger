extends ../layout

block content
  .page-header
    h3 Sign up
  form.form-horizontal(id='signup-form', method='POST')
    input(type='hidden', name='_csrf', value=_csrf)
    .form-group
      label.col-sm-4.control-label(for='email') User name (choose wisely, it can't be changed!)
      .col-sm-6
        input.form-control(type='text', name='userName', id='userName', placeholder='User name', autofocus, required)
    .form-group
      label.col-sm-4.control-label(for='email') Email
      .col-sm-6
        input.form-control(type='email', name='email', id='email', placeholder='Email', required)
    .form-group
      label.col-sm-4.control-label(for='password') Password
      .col-sm-6
        input.form-control(type='password', name='password', id='password', placeholder='Password', required)
    .form-group
      label.col-sm-4.control-label(for='confirmPassword') Confirm Password
      .col-sm-6
        input.form-control(type='password', name='confirmPassword', id='confirmPassword', placeholder='Confirm Password', required)
    .form-group
      .col-sm-offset-4.col-sm-6
        button.btn.btn-success(type='submit')
          i.fa.fa-user-plus
          | Signup
